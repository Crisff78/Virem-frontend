{
  "framework": null,
  "installCommand": "npm install --omit=optional",
  "buildCommand": "npx expo export --platform web --output-dir dist-web --clear",
  "outputDirectory": "dist-web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
